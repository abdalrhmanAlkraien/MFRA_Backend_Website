# MFRA_Backend_Website
MFRA backend website 


openclaw /execute-task        # execute next pending task
openclaw /continue-tasks      # keep executing until all done
openclaw /review-progress     # show completed vs pending
openclaw /fix-task 2.1        # fix a failed task
openclaw /add-feature "..."   # add new feature mid-project
```
claude-project-template/
│
├── .claude/
│   │
│   ├── CLAUDE.md                              ← Master context file (project brain)
│   ├── systemTasks.md                         ← Empty — SpecKit fills this
│   ├── tasks.md                               ← Page-level task tracker (design → done)
│   │
│   ├── configurations.md                      ← Config tracker (cache, email, sms...)
│   │
│   ├── project/                               ← YOU fill these per project
│   │   ├── users.md                           ← User types + permissions
│   │   ├── pages.md                           ← Page list + access + API mapping
│   │   └── stack.md                           ← Tech stack + providers + env vars
│   │
│   ├── instructions/                          ← Reusable forever — write once
│   │   │
│   │   ├── ── Core (always read) ──
│   │   ├── backend.md                         ← Backend coding standards
│   │   ├── frontend.md                        ← Frontend coding standards
│   │   ├── backend-testing.md                 ← JUnit + integration + curl tests
│   │   ├── frontend-testing.md                ← Playwright E2E test standards
│   │   ├── database.md                        ← DB conventions + Flyway rules
│   │   ├── pages.md                           ← BA & PM workflow — image → requirements → tasks
│   │   │
│   │   ├── ── Conditional (read when needed) ──
│   │   ├── async.md                           ← @Async, jobs, scheduled tasks
│   │   ├── cache.md                           ← Redis / Memcached — check stack.md
│   │   ├── email.md                           ← JavaMailSender + templates
│   │   ├── sms.md                             ← Twilio / AWS SNS — check stack.md
│   │   ├── notification.md                    ← FCM / VAPID push — check stack.md
│   │   ├── whatsapp.md                        ← WA Cloud API / Twilio WA — check stack.md
│   │   └── websocket.md                       ← STOMP / SSE — check stack.md
│   │
│   └── commands/                              ← Agent command definitions
│       ├── AI-AGENT-EXECUTION-GUIDE.md        ← OpenClaw reads this FIRST
│       ├── execute-task.md
│       ├── continue-tasks.md
│       ├── review-progress.md
│       ├── fix-task.md
│       ├── add-feature.md
│       │
│       ├── ── SpecKit generates after `specify init` ──
│       ├── speckit.constitution.md
│       ├── speckit.specify.md
│       ├── speckit.plan.md
│       ├── speckit.tasks.md
│       ├── speckit.implement.md
│       └── speckit.checklist.md
│
├── designs/                                   ← YOU drop images + write specs here
│   ├── PAGE_TEMPLATE.md                       ← Copy this for each new page
│   ├── 1.png                                  ← design image (any number, any extension)
│   ├── 1.md                                   ← page spec (same name as image)
│   ├── 1-requirements.md                      ← generated by agent after analysis
│   └── .gitkeep
│
├── .specify/                                  ← SpecKit generates after init
│   ├── memory/
│   │   └── constitution.md
│   └── scripts/
│       ├── create-new-feature.sh
│       └── check-prerequisites.sh
│
├── specs/                                     ← SpecKit writes per module
│   └── .gitkeep
│
└── README.md                                  ← How to use the template



ROUND 1 — Core Coding Standards (reusable forever, write once)
──────────────────────────────────────────────────────────────
1.  instructions/backend.md                ← Patterns, rules, naming, security
2.  instructions/frontend.md               ← RTK Query, Tailwind, RTL, TypeScript
3.  instructions/backend-testing.md        ← JUnit, MockMvc, Testcontainers, curl
4.  instructions/frontend-testing.md       ← Playwright, scenarios, data-testid
5.  instructions/database.md               ← Flyway, conventions, indexes, soft delete
6.  instructions/pages.md                  ← BA & PM workflow — image → requirements → tasks

ROUND 2 — Conditional Instruction Files (write once, reused when triggered)
────────────────────────────────────────────────────────────────────────────
7.  instructions/async.md                  ← @Async, @Scheduled, job status tracking
8.  instructions/cache.md                  ← Redis / Memcached — checks stack.md first
9.  instructions/email.md                  ← JavaMailSender, Mailhog, templates
10. instructions/sms.md                    ← Twilio / AWS SNS — checks stack.md first
11. instructions/notification.md           ← FCM / VAPID — checks stack.md first
12. instructions/whatsapp.md               ← WA Cloud API / Twilio — checks stack.md first
13. instructions/websocket.md              ← STOMP / SSE — checks stack.md first

ROUND 3 — Agent Execution Commands (OpenClaw workflow)
──────────────────────────────────────────────────────
14. commands/AI-AGENT-EXECUTION-GUIDE.md   ← Most important file — OpenClaw reads first
15. commands/execute-task.md
16. commands/continue-tasks.md
17. commands/review-progress.md
18. commands/fix-task.md
19. commands/add-feature.md

ROUND 4 — Trackers
────────────────────
20. .claude/configurations.md              ← Config tracker (✅ READY / ❌ MISSING)
21. .claude/tasks.md                       ← Page task tracker (design → requirements → done)

ROUND 5 — Project Input Templates (you fill per project)
─────────────────────────────────────────────────────────
22. project/stack.md                       ← Tech stack + all providers
23. project/users.md                       ← User types + permissions
24. project/pages.md                       ← Page list + access + API mapping

ROUND 6 — Master Context File
──────────────────────────────
25. CLAUDE.md                              ← References all above — project brain

ROUND 7 — Empty Placeholders + Design Template
────────────────────────────────────────────────
26. systemTasks.md                         ← Empty — SpecKit fills this
27. designs/PAGE_TEMPLATE.md               ← Copy this for each new page
28. designs/.gitkeep
29. specs/.gitkeep

ROUND 8 — Run SpecKit (auto-generates these)
─────────────────────────────────────────────
30. specify init . --ai claude
    → commands/speckit.constitution.md
    → commands/speckit.specify.md
    → commands/speckit.plan.md
    → commands/speckit.tasks.md
    → commands/speckit.implement.md
    → commands/speckit.checklist.md
    → .specify/memory/constitution.md
    → .specify/scripts/*.sh

ROUND 9 — Final
────────────────
31. README.md                              ← How to use the template

```

# Claude Project Template

A reusable AI agent automation framework for building full-stack applications
with Spring Boot, React, and Flutter — powered by OpenClaw and SpecKit.

Drop design images. Write a short page spec. The agent analyzes requirements,
generates tasks, writes code, runs tests, and tracks everything automatically.

---

## What This Template Does

```
You drop:    designs/1.png + designs/1.md
             ↓
Agent runs:  /analyze-page 1
             → reads image + spec
             → extracts DB schema, API endpoints, business rules
             → writes designs/1-requirements.md
             → generates tasks in systemTasks.md
             ↓
Agent runs:  /generate-spec consultation
             → produces specs/consultation/spec.md + plan.md + tasks.md
             ↓
Agent runs:  /execute-task
             → asks: backend / frontend / mobile / all?
             → builds entity + service + controller + migration
             → builds pages + components + RTK Query
             → runs JUnit + Playwright tests
             → writes documentation
             → updates all tracking files
```

---

## Supported Stack

| Layer | Technology |
|---|---|
| Backend | Spring Boot 3.x + Java 21 |
| Frontend | React 18 + TypeScript + Vite + RTK Query + Tailwind |
| Mobile | Flutter (optional — if listed in stack.md) |
| Database | PostgreSQL + Flyway |
| Cache | Redis (optional) |
| Storage | AWS S3 (optional) |
| Email | SMTP + Thymeleaf (optional) |
| Auth | JWT stateless |

All providers are optional and controlled by `project/stack.md`.
The agent only builds what is listed there.

---

## Folder Structure

```
claude-project-template/
│
├── .claude/
│   ├── CLAUDE.md                    ← Project brain — read this first
│   ├── systemTasks.md               ← All tasks with status + cost tracking
│   ├── tasks.md                     ← Per-page stage tracker + files built
│   ├── configurations.md            ← Config readiness (Redis, Email, S3...)
│   │
│   ├── project/                     ← YOU fill these per project
│   │   ├── stack.md                 ← Tech stack + providers + env vars
│   │   ├── users.md                 ← User types + permissions + security rules
│   │   └── pages.md                 ← Page list + URLs + module names
│   │
│   ├── instructions/                ← Reusable coding standards — write once
│   │   ├── backend.md               ← Spring Boot patterns + rules
│   │   ├── frontend.md              ← React + RTK Query + Tailwind patterns
│   │   ├── backend-testing.md       ← JUnit + MockMvc + Testcontainers
│   │   ├── frontend-testing.md      ← Playwright E2E test patterns
│   │   ├── database.md              ← Flyway + entity + repository conventions
│   │   ├── pages.md                 ← BA & PM image analysis workflow
│   │   ├── async.md                 ← @Async + @Scheduled patterns
│   │   ├── cache.md                 ← Redis caching patterns
│   │   ├── email.md                 ← JavaMailSender + templates
│   │   ├── sms.md                   ← Twilio / AWS SNS
│   │   ├── notification.md          ← FCM / VAPID push
│   │   ├── whatsapp.md              ← WhatsApp Cloud API / Twilio WA
│   │   └── websocket.md             ← STOMP / SSE
│   │
│   └── commands/                    ← Agent slash commands
│       ├── AI-AGENT-EXECUTION-GUIDE.md  ← Master execution workflow
│       ├── analyze-designs.md       ← Analyze all design images at once
│       ├── analyze-page.md          ← Analyze a single design image
│       ├── generate-spec.md         ← Requirements → SpecKit spec
│       ├── check-ready.md           ← Pre-flight validation
│       ├── execute-task.md          ← Execute the next task
│       ├── continue-tasks.md        ← Run all tasks automatically
│       ├── fix-task.md              ← Fix a failed or blocked task
│       ├── review-progress.md       ← Full project status report
│       └── add-feature.md           ← Add a new feature mid-project
│
├── designs/                         ← YOU drop images + write specs here
│   ├── PAGE_TEMPLATE.md             ← Copy this for each new page
│   ├── 1.png                        ← Design image
│   ├── 1.md                         ← Page spec (you write)
│   ├── 1-requirements.md            ← Generated by agent
│   └── ...
│
├── specs/                           ← SpecKit generates per module
│   └── <module>/
│       ├── spec.md
│       ├── plan.md
│       └── tasks.md
│
└── README.md
```

---

## Setup — New Project in 4 Steps

### Step 1 — Copy the Template

```bash
cp -r claude-project-template/.claude ./your-project/.claude
cp -r claude-project-template/designs ./your-project/designs
```

### Step 2 — Fill 4 Files (~30 minutes)

**`project/stack.md`** — your technology choices:
```yaml
Backend:   Spring Boot 3.x
Frontend:  React 18 + TypeScript
Mobile:    ⛔ not in this project
Database:  PostgreSQL
Cache:     Redis
Email:     SMTP
Storage:   AWS S3
```

**`project/users.md`** — who can do what:
```
PUBLIC    → any visitor — no auth
ADMIN     → full access to admin panel
EDITOR    → content management only
```

**`project/pages.md`** — your page list with URLs and module names.

**`.claude/CLAUDE.md`** — project name, company, budget, architecture rules.

### Step 3 — Validate

```
/check-ready
```

Shows a 9-layer checklist. Fix any ❌ blocking issues before continuing.

### Step 4 — Add Designs and Start

```bash
# For each page:
cp designs/PAGE_TEMPLATE.md designs/1.md
# Fill in 1.md — business purpose, access, permissions, notes
# Drop 1.png into designs/

# Then run:
/analyze-page 1
```

---

## Daily Workflow

### Adding a new page

```bash
# 1. Drop the image
designs/3.png

# 2. Write the spec
cp designs/PAGE_TEMPLATE.md designs/3.md
# Fill in business description, access, permissions

# 3. Analyze
/analyze-page 3

# 4. Generate spec (optional but recommended)
/generate-spec <module-name>

# 5. Build
/execute-task
```

### Running all pending tasks

```
/continue-tasks
```

Agent asks once which platforms to build, then runs every pending task
in dependency order, auto-fixes failures (2 attempts), and stops only
when done or budget is low.

### Checking project status

```
/review-progress
```

Shows progress bar, per-phase breakdown, test summary, config status, budget.

### Fixing a failed task

```
/fix-task 3.1
```

Agent reads the failure, diagnoses whether it is a code bug or a
requirements issue, fixes the right layer, updates all tracking files.

### Adding a feature mid-project

```
/add-feature "Add a partners logo grid to the home page"
```

Agent analyzes impact across all files, shows what will change, waits
for confirmation, then generates requirements and tasks.

### Updating a design after development started

```bash
# Replace the image
designs/1.png  ← updated design

# Re-analyze
/analyze-page 1
# → shows a diff of what changed
# → only touches files built from page 1
# → never touches files from other pages
```

---

## The Page Spec File

Copy `designs/PAGE_TEMPLATE.md` for each new page and fill in:

```markdown
# [Page Name]

## Business Description
What does this page do? What problem does it solve?

## User Access
- [ ] PUBLIC — any visitor, no login
- [ ] ADMIN — logged-in admin user
- [ ] EDITOR — logged-in editor user

## Permissions
View:   [what is visible]
Create: [what can be submitted]
Edit:   [what can be changed]
Delete: [what can be removed]
Other:  [publish, reorder, export, toggle, etc.]

## Related Pages
[page number or name] → [why related]

## Notes
Special behavior, business rules, edge cases, out-of-scope items.
```

The agent reads this file before opening the image. The more context
you provide here, the less clarifying questions it needs to ask.

---

## Slash Commands

| Command | What It Does |
|---|---|
| `/check-ready` | Validates project setup — run before anything else |
| `/analyze-designs` | Analyzes all design images at once |
| `/analyze-page [N]` | Analyzes a single design image — safe to run mid-project |
| `/generate-spec [module]` | Converts requirements to SpecKit spec files |
| `/execute-task` | Executes the next pending task |
| `/continue-tasks` | Runs all pending tasks without stopping |
| `/fix-task [id]` | Fixes a failed, blocked, or unclear task |
| `/review-progress` | Full project status snapshot |
| `/add-feature "..."` | Adds a new feature or page mid-project |

---

## Tracking Files

### `tasks.md` — Per-Page View

Tracks every page through 5 stages:

```
| Page | Design | Requirements | Backend | Frontend | Mobile |
|------|--------|--------------|---------|----------|--------|
| 1    | ✅     | ✅           | ✅      | 🔄       | ⛔     |
| 2    | ✅     | ✅           | ⏳      | ⏳       | ⛔     |
| 3    | ✅     | ✅           | ⏳      | ⏳       | ⛔     |
```

Each page entry includes the exact list of files built — used to scope
re-analysis safely when a design is updated.

### `systemTasks.md` — Per-Task View

Tracks every task with:
- Status (⏳ Pending / 🔄 In Progress / ✅ Done / ❌ Failed)
- Source page (which image it came from)
- Dependencies
- Token usage and cost
- Test results summary

### `configurations.md` — Config Readiness

```
✅ Database          → set up in Task 1.1
✅ Security / JWT    → set up in Task 1.2
❌ Email             → needed by Page 7 (Consultation)
❌ File Storage      → needed by Page 3 (Blog editor)
```

---

## Status Legend

| Symbol | Meaning |
|---|---|
| ✅ | Done and verified |
| ⏳ | Not started |
| 🔄 | In progress |
| ❌ | Failed — run /fix-task |
| ⚠️ | Needs review — human decision required |
| ⛔ | Not in scope for this project |
| 🔒 | Blocked — dependency not complete |

---

## Reusable vs Project-Specific

### Write Once — Never Change Per Project
```
.claude/instructions/   ← all 13 instruction files
.claude/commands/       ← all 10 command files
designs/PAGE_TEMPLATE.md
```

### Fill Per Project (~30 min)
```
.claude/project/stack.md
.claude/project/users.md
.claude/project/pages.md
.claude/CLAUDE.md
```

### Generated by Agent (never edit manually)
```
designs/[N]-requirements.md
.claude/tasks.md
.claude/systemTasks.md
specs/<module>/spec.md
specs/<module>/plan.md
specs/<module>/tasks.md
.claude/processed/
doc/DATABASE_AUDIT.md
```

---

## Quality Gates

Every task must pass all of these before being marked ✅:

**Backend:**
- `mvn clean verify` → BUILD SUCCESS, 0 failures
- All admin endpoints have `@PreAuthorize`
- Soft delete verified — `deletedAt` set, never hard delete
- All curl smoke tests verified against DB (`DATABASE_AUDIT.md`)

**Frontend:**
- `npm run build` → 0 TypeScript errors
- All admin routes wrapped in `AuthGuard`
- Loading, error, and empty states handled
- Playwright tests pass on Desktop + Tablet + Mobile

**Mobile (if in stack):**
- `flutter analyze` → 0 issues
- All widget tests pass

**Documentation:**
- `.claude/processed/Task X.Y.md` created
- `.claude/processed/Task X.Y - Test Results.md` created
- `tasks.md` updated — stage status + files built
- `systemTasks.md` updated — status + cost

---

## Tips

**Keep page specs concise.** The agent reads the full image — you only need
to add what is not visible: business rules, edge cases, out-of-scope items.

**Run `/check-ready` before starting.** Catches missing files, unfilled
templates, and budget issues before any work begins.

**Use `/analyze-page` for one page at a time.** It is safer and faster than
running the full analysis when you are adding pages incrementally.

**Let the agent ask clarifying questions.** If the spec says "preferred meeting
time" without format details, the agent will ask. Answer it — the answer gets
saved to `requirements.md` so it is never asked again.

**Never edit generated files manually.** `requirements.md`, `systemTasks.md`,
`tasks.md`, and `specs/` are all agent-owned. Edit `designs/[N].md` and
`project/*.md` instead — those are yours.

**The "Files Built" section in `tasks.md` is critical.** It is automatically
filled when tasks complete. Without it, updating a design image mid-project
cannot be done safely.

---

## Requirements

- Claude Code or OpenClaw
- SpecKit (`npm install -g specify-cli`)
- Java 21 + Maven (for Spring Boot projects)
- Node 18+ (for React frontend)
- Flutter 3.x (if mobile is in scope)
- Docker + Docker Compose (local dev: PostgreSQL, Redis, Mailhog)
